[metadata]
name = moving_table_control
version = 0.0.0

[options]
zip_safe = True
install_requires =
    setuptools

[options.entry_points]
console_scripts =
    dual_table_controller = moving_table_control.dual_table_controller:main